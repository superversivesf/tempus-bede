# Bun configuration for tempus-bede

[test]
# Test configuration - preload will be added when src/ is created
# preload = ["src/test-setup.ts"]

[install]
# Use exact versions for reproducibility
exact = true

[run]
# Silent mode for scripts
silent = false